<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.football.freekick">

    <!-- 手机震动 -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- 获取手机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <!-- &lt;!&ndash; 用于进行网络定位 &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/> -->
    <!-- &lt;!&ndash; 用于访问GPS定位 &ndash;&gt; -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> -->
    <!-- 访问wifi网络信息,wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- 操作摄像头 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>
    <!-- 允许应用读取低级别的系统日志文件 -->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <!-- 操作键盘锁 -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <!-- 读取联系人 -->

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/icon_desktop"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_desktop_round"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme">
        <meta-data
            android:name="design_width"
            android:value="750"/>
        <meta-data
            android:name="design_height"
            android:value="1334"/>

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".TestActivity">
        </activity>
        <activity android:name=".RegisterLoginActivity">
        </activity>
        <activity
            android:name=".language.SelectLanguageActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert"/>
        <activity
            android:name=".CalenderActivity"
            android:theme="@style/MyCalendar">
        </activity>
        <activity android:name=".activity.registerlogin.RegisterPager1Activity">
        </activity>
        <activity android:name=".activity.registerlogin.RegisterPager2Activity">
        </activity>
        <activity android:name=".activity.registerlogin.FirstPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 注册 存储照的容器 -->
        <!--
        name:属性值，固定写法
        authorities:组件标识，按照江湖规矩,都以包名开头,避免和其它应用发生冲突。和FileProvider.getUriForFile()方法的第二个参数一致
        exported:要求必须为false，为true则会报安全异常。
        grantUriPermissions:true，表示授予 URI 临时访问权限。
        -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.football.freekick.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <!-- 指定Uri的共享路径 -->
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".activity.registerlogin.RegisterPager3Activity">
        </activity>
        <activity android:name=".activity.registerlogin.OneTimePagerActivity">
        </activity>
        <activity android:name=".activity.registerlogin.LoginPage1Activity">
        </activity>
        <activity android:name=".activity.registerlogin.LoginPager2Activity">
        </activity>
        <activity android:name=".activity.registerlogin.RegisterByEmailActivity">
        </activity>
        <activity
            android:name=".activity.ChooseTimeActivity"
            android:theme="@style/MyCalendar">
        </activity>
        <activity android:name=".activity.MatchInviteActivity">
        </activity>
        <activity
            android:name=".activity.FiltrateActivity"
            android:theme="@style/MyDialogStyleLeft">
        </activity>
        <activity android:name=".activity.ArticleActivity">
        </activity>
        <activity android:name=".activity.MatchRateActivity">
        </activity>
        <activity android:name=".activity.MatchContentActivity">
        </activity>
        <activity android:name=".activity.TeamDetailActivity">
        </activity>

        <service android:name=".service.MyInstanceIDListenerService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>
        <service android:name=".service.MyFcmListenerService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <!--
     Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.firebase.messaging.default_notification_icon" -->
        <!-- android:resource="@drawable/ic_stat_ic_notification" /> -->
        <!--
             Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent"/>

        <activity android:name=".activity.JoinMatchActivity">
        </activity>
        <activity android:name=".activity.ShowMatchActivity">
        </activity>
    </application>

</manifest>